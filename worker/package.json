{
  "name": "nexiup-telegram-gateway",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker webhook gateway for the NexiUP Telegram bot",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "logs": "wrangler tail --format pretty",
    "setup": "node scripts/setup.mjs",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  },
  "engines": {
    "node": ">=18"
  }
}
