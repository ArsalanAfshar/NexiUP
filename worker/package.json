{
  "name": "nexiup-telegram-gateway",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker gateway: receives the Telegram webhook, answers 200 immediately, and forwards updates to the NexiUP Google Apps Script backend.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "logs": "wrangler tail --format pretty",
    "secrets": "wrangler secret put APPS_SCRIPT_SECRET && wrangler secret put TELEGRAM_WEBHOOK_SECRET"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
